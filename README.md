# task-1-mars
for easy part i learnt the linux commands which include 

mkdir: to create directory

cd: to change the directory

touch: used to add files inside a directory

ls: to list the files inside a folder 

find . -iname: to find all the files with an extension 

cat> file: to display the text inside the file

grep: used to print all the lines in a file containing a KEYWORD

wc -l: to count the occurrence of a word

date: date and time is displayed 

top: shows the overall cpu usage

sudo shutdown -h time: commands the computer to shutdown after a given time

echo:used to display a text just like print function 

exit: exits the program 

​#!/bin/bash:script should be run using the Bash interpreter.

ping -c 1 &> /dev/null google.com: Tests the internet connection silently without showing output.

for the working of rover, the Linux os is used and this task helped me to get a hands on experience about the Bash script and the command in Linux.

MEDIUM:Q1:
went through the concept of frame of reference and read about the rotation matrix and how it can be used to convert the coordinate from the camera frame to rover frame and then to the world frame.

rotation matrix was a totally new concept. But, i had an aidea about the rotation of coordinates in 2D. 
This was an extension of it.

Medium Q2.
This helped me to explore a new code language which consists of dashes and dots called the morse coding language i assigned each letter,number and some special characters with appropriate morse code and then took the input from the user and separated each letter and decoded it separately using strtoken.

MEDIUM Q3.
In the third question i used the concept of ascii values of the letters and arrays the first letter's index is decreased by 1 similarly the 2nd is decreased by 2 and so on to get the encrypted code figured out.

It was sort of easier than the first question. Since this is a well known concept in aptitude coding decoding.

Medium Q4.
It was about the use of two filters muchiko filter that is the mean filter, first of all it will sort the array and then access the first three elements out of the array and find the mean then the next three and so on and store it in another result array.

The second filter was Sanchiko filter which was the mean filter. This didnt require sorting of the array it simply removed all the noise and only one number was left. It is better than the mean filter since it is able to filter out all the noise.

Med Q5.
This was quite a challenging question for me since it made use of lots of concepts including data structures, pointers also there were many conditions assigned. 
The most interesting thing in this the rover to remeber it's memory of earlier movement position and at the same time detect the smallest wear cost which is an accumulation of the other wear costs.

Hard Q1.
It comprised of the concept of array and the general coordinate system. first i defined the origin of the grid which varies according to its size.
And then detected the obstacle point using east-west and north-south for each obstacle.
I did this to get all the four quadrants of the coordinate axis. The coordinate system i created is 90 degree rotated. y axis in vertical direction and y axis on horizontal.And then i printed the array using 3 conditions i.e. *,0,1.

Hard Q3:
behaviour tree, i explored the AND, OR cases used in the tree and how the rover will decide which path to choose using a simple method rather than a complicated if else statement.

Overall I got to learn a lot using this task making use of the basic codes i learnt into codes that might be used in the real life scenario.

to create the github repository 
i created an account using my Gmail and click of the left dashboard 
there is an option for creating new repository
I clicked on it 
then I uploaded the documents containing codes as well as the jpg files 
and saved it
I enabled the settings to public view.






